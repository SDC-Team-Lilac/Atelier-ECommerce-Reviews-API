version: '3'

services:
  db:
      image: 'postgres'
      ports:
        - '5432:5432'
      environment:
        POSTGRES_PASSWORD: 'postgres'
        POSTGRES_USER: 'postgres'
        POSTGRES_DB: 'postgres'
        POSTGRES_HOST_AUTH_METHOD: 'trust'
      volumes:
        - db:/var/lib/postgresql/data
        - /Users/jccode/HR/rpp2210-sdc-lilac-reviews/data:/var/lib/postgresql/data/reviewdata
  server:
    image: my-app
    build: .
    ports:
      - 3001:3001
    environment:
      POSTGRES_PASSWORD: 'postgres'
      POSTGRES_USER: 'postgres'
      POSTGRES_DB: 'postgres'
    depends_on:
      - db

volumes:
  db: